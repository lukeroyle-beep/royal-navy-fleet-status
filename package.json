{
  "name": "royal-navy-osint-fleet-map",
  "version": "0.2.0",
  "private": true,
  "description": "Browser-based map of last publicly reported Royal Navy and RFA vessel locations.",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "validate:data": "node scripts/validate-fleet-data.mjs",
    "test": "node scripts/test-loader.mjs && node scripts/test-map.mjs",
    "build": "npm run validate:data && npm test && vite build && node scripts/copy-fleet-data.mjs",
    "preview": "vite preview --host 127.0.0.1"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  },
  "dependencies": {
    "leaflet": "^1.9.4",
    "leaflet.markercluster": "^1.5.3"
  }
}
